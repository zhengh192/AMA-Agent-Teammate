# Governed Skills

Only approved, versioned Skills become active. Phase 3 will add `skills/<skill_name>/SKILL.md`, metadata, examples, tests, queries, and scripts through an approval-controlled proposal workflow.
