---
name: period-comparison
description: Compare an approved metric across explicit current and baseline periods.
---

# Period comparison

Require a metric, current period, comparable baseline, time grain, and entity scope. Verify equal coverage and calendar logic. Calculate absolute change, percentage-point change for rates, and relative change deterministically. State when a zero baseline makes relative change undefined.
