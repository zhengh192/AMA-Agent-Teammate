# API application placeholder

Phase 1 will create the FastAPI application and Python package under `src/ama_teammate/`. Keep orchestration, domain, provider, connector, policy, storage, and API boundaries explicit. No runnable backend is claimed in Phase 0.
