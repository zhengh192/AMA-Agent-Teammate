# Web application placeholder

Phase 1 will create the React + Vite + TypeScript chat interface here. Agent roles remain backend concepts; the user sees tasks, plans, approvals, evidence, status, and safe errors.
