# Infrastructure placeholder

Infrastructure starts with reproducible local development and approved deployment configuration. Kubernetes is explicitly outside the MVP. Never place secrets in this directory.
